# Demo 1 — Copilot CLI

Shows `gh copilot suggest` and `gh copilot explain` as an AI-powered terminal companion for the TechMart developer workflow.

## Setup

```bash
gh extension install github/gh-copilot
cd /path/to/nyc-dev-days
```

No other setup required.
